const SELECTED = 'SELECTED'
const UNSELECTED = 'UNSELECTED'

export const createRect = (dimensions) => {
    const _state = {
        dimensions
    }

    const machine = {
        state: UNSELECTED,
        option: {

        }
    }
}

